{
	"name": "@widgetable/types",
	"version": "0.1.0",
	"private": true,
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsc -b",
		"dev": "echo \"no dev script for types\"",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"format": "prettier --write \"src/**/*.{ts,tsx}\" || true",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"typescript": "^5.0.0"
	}
}